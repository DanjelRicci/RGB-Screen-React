-- tables.lua

local tables = {}

tables.settings = {
    mode = 0,         -- 0 = Off, 9 = On (Screen React)
    brightness = 7,   -- Scale of 1 to 10
}

tables.menu = {"Enabled", "Brightness"}

return tables
